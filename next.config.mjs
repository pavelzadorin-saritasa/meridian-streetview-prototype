/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    // distDir: 'docs',
    // basePath: '/meridian-streetview-prototype',
};

export default nextConfig;
